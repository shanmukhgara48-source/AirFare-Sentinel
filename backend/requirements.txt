fastapi==0.141.1
pydantic==2.13.5
pydantic_core==2.46.5
python-multipart==0.0.32
starlette==1.6.0
uvicorn==0.52.4
# Optional: load .env files (python-dotenv). Falls back to OS env if not installed.
python-dotenv>=1.0.0
# Starlette 1.6 prefers httpx2 for TestClient; keeping httpx supports FastAPI's
# current optional dependency while avoiding the compatibility warning.
httpx>=0.27.0
httpx2>=2.0.0
